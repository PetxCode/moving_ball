import React from "react";

const MainScreen = () => {
  return (
    <div className="h-screen bg-red-100 justify-center flex items-center w-full">
      MainScreen
    </div>
  );
};

export default MainScreen;
